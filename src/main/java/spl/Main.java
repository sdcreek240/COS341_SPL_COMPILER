package spl;

/*
This was jsut for a test of nodes
import spl.tree.RootNode;
import spl.tree.InnerNode;
import spl.tree.TerminalNode;
import spl.tree.TreeFactory;
*/
public class Main {
    public static void main(String[] args) {
        System.out.println("SPL Compiler");
        /*
           TreeFactory factory = new TreeFactory();
           RootNode root = factory.getTree();
           InnerNode branch = factory.getInner("branch-1");
           TerminalNode leaf1 = factory.getTerminal("leaf-1");
           TerminalNode leaf2 = factory.getTerminal("leaf-2");
           root.addChild(branch);
           branch.addChild(leaf1);
           branch.addChild(leaf2);
           
           System.out.println("Root content: " + root.getContent());
        */
    }
}